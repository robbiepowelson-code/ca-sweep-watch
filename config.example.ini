# Copy to config.ini and fill in. config.ini is gitignored — NEVER commit it.
# PRIVACY: use a dedicated email (e.g. records@yourproject.org) and a PO box
# or org address, not your home address. Agencies' responses become public
# records too.

[requester]
name = Records Requester
org = CA Sweep Watch
email = records@example.org
mailing_address = PO Box 0000, City, CA 00000
fee_cap = 25

[smtp]
host = smtp.example.org
port = 587
user = records@example.org
password = CHANGE_ME
from_addr = records@example.org
